class Solution:
    def merge(self, nums1: List[int], m: int, nums2: List[int], n: int) -> None:
        """
        Do not return anything, modify nums1 in-place instead.
        """
        mn=m+n
        for i in range(m,mn):
            nums1[i]=nums2[mn-i-1]
        nums1=nums1.sort()                       

                       
                    
                    
                    

        
